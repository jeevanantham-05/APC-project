#include<stdio.h>
#include"header.h"

void add(Dlist *tail1, Dlist *tail2)
{
    //create final list
    Dlist *final = malloc(Dlist);
    if(!final) return fail;

    Dlist *temp = 
    while()
    
}